#include "BulletAI.h"

USING_NS_CC;

BulletAI::BulletAI(const cocos2d::Vec2 &velocity, const cocos2d::Vec2 &acceleration) :
    _velocity(velocity), _acceleration(acceleration)
{
}

void BulletAI::update(GameEntity * target)
{
    
}